#include <stdio.h>
int main()
{
    int num1, num2, difference;
    printf ("Enter two digits to get the difference: ");
    scanf ("%d %d", &num1, &num2);
    difference = num2 - num1;
    printf("The difference of two numbers are %d",difference);

}
